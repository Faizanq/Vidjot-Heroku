{{#each ideas}}
	
	<div class="card card-body mb-2">
		<h2>{{title}}</h2>
		<p>{{details}}</p>
		<a href="/ideas/edit/{{id}}" class="btn btn-dark btn-block">Edit</a>
	</div>

{{else}}

	<h2>No ideas </h2>

{{/each}}